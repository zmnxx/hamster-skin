元书皮肤

一套深色为主的中文九键皮肤。

键盘构成

主键盘是中文九键（T9）。另外包含 26 键中文、26 键英文、数字九宫格、
符号键盘、浮动功能面板，横屏竖屏与 iPad 都有对应布局。

浮动功能面板

工具栏最左侧的「菜单」键唤出浮动功能面板，共三行十二个按键：

第一行是设置、皮肤、方案、状态。
第二行是文件、脚本、内嵌、内存。
第三行是 QQ、微信、抖音、拼多多四个 App 快捷入口，
点了会跳转到对应 App，没装那个 App 时点了不会有反应。

面板是一块半透明玻璃板，下方键盘会隐约透出。点面板以外的区域即可收起。

外观特点

按键是上下渐变加一圈细描边，底部有一条暗边做出立体感，按下时有光晕与
涟漪两层动效。键盘背景完全透明，交给系统自带的键盘背板，所以在不同
App 里都能和系统外观贴合。

皮肤固定使用深色配色。有些 App 会强制把输入法拉成浅色，这套皮肤在
那些 App 里也保持深色，不会突然变白。

九键怎么用

左侧那一列在没有输入时是符号栏，开始打字后会变成拼音选择列。
符号栏显示的内容跟随「键盘设置 - 中文九键符号设定」，想换符号请在
元书设置里改。

左下角是符号键盘与数字键盘的切换键。
长按字母键会弹出该键的符号面板，手指朝目标方向滑动后松手即可选中。
九键按键没有短按放大气泡。

打字时空格上划选第二个候选，下划选第三个候选。不打字时空格上下划无动作，
这样不会误触。

1 键在不打字时是 @ 与常见邮箱后缀，开始打字后自动变成分词键。

功能行

功能行在不打字时是文本编辑：左移、行首、全选、剪切、复制、粘贴、行尾、右移。

开始打字后整行切换成输入辅助：

左右两端是移动光标，「候选」用来展开或收起全屏候选列表。
「次选」「三选」直接上屏第二、第三个候选字。
「词首」「词尾」是以词定字，先打一个词，再取它的第一个或最后一个字上屏。
九键打单字重码多，用这个方式取字更准。

以词定字需要输入方案配合。雾凇拼音的中文九键方案默认没有启用它，
需要在方案补丁里加载 select_character 脚本，并把取字快捷键设为左右中括号。

工具栏

工具栏是一条通长的胶囊，八个按钮固定显示，不需要左右滑动：
菜单、搜索、网址、商店、常用、剪贴、脚本、收起。

打字时工具栏会被候选栏替换。点候选栏右侧的按钮可以展开成全屏候选列表，
展开后底部有翻页、返回、退格四个键。

符号键盘与数字键盘

数字键盘（123）与符号键盘左下角都有返回键，一律回到中文主键盘。
无论你是从九键、26 键中文还是英文键盘进来的，返回键的去向都一样，
不会把你丢回英文键盘。

用九宫格时返回落到九宫格，用 26 键全拼时返回落到 26 键全拼，
跟随你在皮肤配置里选的中文键盘布局。

按键音

皮肤自带一套按键音，全部键盘统一使用：输入键一个音，系统键（回车、
切换等）一个音，空格单独一个音，删除键一个音。

按键音要在元书里打开才会响：键盘设置里启用按键音，并把音效来源设为
跟随皮肤。系统静音开关关掉声音时同样不会响。

元书本身没有按键音音量选项，声音大小取决于音频文件本身，
以及系统的媒体音量。

按键音需要元书 1.6.20 及以上版本（TF 386 以上），更早的版本会忽略这项配置，
键盘只发系统默认音。

中英文切换

工具栏最右侧偏左的位置有中英切换键。英文键盘左下角的按键点一下切回中文。
中英文两侧的 26 键布局、划动内容与回车文案完全一致，不存在第二种版本。

已知问题

有少数 App 会强制输入法进入浅色模式，此时九键左侧符号栏的文字会变成
黑色。这一列的文字颜色由元书自身控制，皮肤无法覆盖，暂时无法修复。
其余区域在这类 App 里显示正常。
